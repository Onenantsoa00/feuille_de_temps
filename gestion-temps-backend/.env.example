PORT=3000
JWT_SECRET=change-me

# Local PostgreSQL
DB_USER=dev_user
DB_HOST=localhost
DB_NAME=gestion_temps
DB_PASSWORD=1234
DB_PORT=5432

# Production PostgreSQL URL (Supabase/Render/etc.)
# If DATABASE_URL is set, DB_* values are ignored.
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Email sender identity
MAIL_FROM=no-reply@feuilledetemp.net

# Optional (recommended) transactional email via Resend API
# Create key at https://resend.com, then set it in dev/prod
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
